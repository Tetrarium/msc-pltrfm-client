import React, { FC } from "react";

import MainLayout from "@/layouts/mainLayout";

const Index: FC = () => {
  return (
    <MainLayout>
      <div>
        Список альбомов
      </div>
    </MainLayout>
  );
};

export default Index;